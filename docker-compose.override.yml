services:
  web:
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8001:8000"
    environment:
      DJANGO_DEBUG: "True"
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
